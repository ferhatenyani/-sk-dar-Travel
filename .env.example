# ——— Neon PostgreSQL (pooled connection string) ———
DATABASE_URL="postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require"

# ——— URL publique du site (sitemap, robots, SEO/JSON-LD) ———
# En prod : https://votre-domaine.netlify.app (ou domaine custom)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# ——— Better Auth ———
BETTER_AUTH_SECRET="" # openssl rand -base64 32
BETTER_AUTH_URL="http://localhost:3000"

# ——— UploadThing (images) ———
UPLOADTHING_TOKEN="" # token SDK v7+ (utdata / eyJ...), dashboard uploadthing.com → API Keys

# ——— Resend (emails) ———
RESEND_API_KEY=""
CONTACT_NOTIFICATION_EMAIL="uskudar.travel19@gmail.com"

# ——— Seed admin initial (pnpm db:seed) ———
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
